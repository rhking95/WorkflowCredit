{% extends 'baseconnect.html.twig'%}
{% block content %}

    <form  action = "{{path('check_Directeur')}}" method="post">

        <div class="col-xs-10 col-xs-offset-1 col-sm-8 col-sm-offset-2 col-md-4 col-md-offset-4">
            <div class="login-panel panel panel-default">
                <div class="panel-heading">Log in</div>

                <div class="panel-body">
                    <table>

                        <tr>
                            <td height="50px" width="500px"><input type="text" name="login" class="form-control" placeholder="Login" autofocus></td>
                        </tr>
                        <tr>
                            <td><input type="password" name="password" class="form-control" placeholder="Password"></td>
                        </tr>
                        <tr>
                            <td height="50px"><font color="red">{{ err3 }}</font></td>
                        </tr>
                        <tr>
                            <td><input type="submit" value="connect" class="btn btn-primary"></td>
                        </tr>
                    </table>
                </div>
            </div>
        </div>
    </form>

{% endblock %}